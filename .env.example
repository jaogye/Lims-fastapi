# Application Settings
APP_NAME=LIMS FastAPI
APP_VERSION=1.0.0
DEBUG=False

# Security Settings
SECRET_KEY=your-super-secret-key-change-in-production-min-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database Settings
DATABASE_HOST=localhost
DATABASE_PORT=1433
DATABASE_NAME=LIMS_PVS
DATABASE_USER="jao"
DATABASE_PASSWORD=""
DATABASE_DRIVER=ODBC Driver 17 for SQL Server

# Alternative: Full database URL
# DATABASE_URL=mssql+pyodbc://sa:YourStrongPassword123!@localhost:1433/LIMS_PVS?driver=ODBC+Driver+17+for+SQL+Server

# CORS Settings
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080", "http://127.0.0.1:3000"]

# File Storage Settings
UPLOAD_DIR=uploads
REPORTS_DIR=reports
TEMP_DIR=temp
MAX_FILE_SIZE=10485760  # 10MB in bytes

# Logging Settings
LOG_LEVEL=INFO
LOG_FILE=app.log

# Pagination Settings
DEFAULT_PAGE_SIZE=50
MAX_PAGE_SIZE=1000

# Background Tasks Settings (Celery + Redis)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Email Settings (Optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=True

# Report Generation Settings
REPORT_TEMPLATES_DIR=templates/reports
SIGNATURE_DIR=signatures

# Business Rules Settings
WORKING_DAYS=[0,1,2,3,4]  # Monday to Friday (0=Monday, 6=Sunday)
SAMPLE_NUMBER_FORMAT={type}{date}{sequence:03d}